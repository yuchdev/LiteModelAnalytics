# LiteModelAnalytics
A Python library and CLI for request-aware LLM model analytics, comparison, and selection. The project is built on top of LiteLLM for normalized model execution and usage/cost handling, but it must not delegate the core analytics or selection policy to LiteLLM.
